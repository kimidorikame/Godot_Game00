# 作業ログ

> 数週間後の自分が読んで「どこまでやったか」を思い出すための記録。
> 実装の詳細は各コミット・コードを見ればわかるので、ここには「経緯」と「保留になっていること」を書く。

---

## 2026-08-01

### 今日やったこと

1. **NEWGAME時のGameStateリセット実装**
   `autoload/game_state.gd` に `reset_for_new_game()` を追加し、`title.gd` のNEWGAME押下時に呼ぶようにした。初期資金は `INITIAL_MONEY` 定数に切り出し済み。
   「翌日へ」（`home/result.gd` の `_on_next_day_pressed`）はリセットせず状態を引き継ぐ、という区別を明確にした。

2. **Dialogic 2導入、夜営業ループにダミー会話を配線**
   `addons/dialogic/` を導入・有効化。`dialogue/d1_roujin.dtl` に老人向けダミー会話（2行）を作成し、`night/game.gd` の `Stage.CONVERSATION` に入ったタイミングで `Dialogic.start("d1_roujin")` を再生、`Dialogic.timeline_ended` で次のステージへ進む配線を通した。
   **客ごとの出し分けはまだ無く、3人とも同じダミー会話が固定で流れる。** 会話の中身は本番用ではない。

3. **M3昼パートの最小縦切り**
   「行き先選択（`scenes/market/DestinationSelect.tscn`）→ 市場（`Shop.tscn`）で仕入れ → 夜の仕込みに在庫が反映される」を通した。
   `RecipeResource.price` をベース2種に設定（豚骨800円・精進500円、仮価格）。`GameState.inventory` に購入個数を積み、`night/game.gd` の仕込み画面は在庫があるベースしか選べず、仕込むと1個消費する。
   行き先は今のところ「市場」1つのみ（ボタン追加だけで増やせる作りにはしてある）。

### 現在のマイルストーン状況

| # | 内容 | 状況 |
|---|---|---|
| M1 | コアロジック | ✅ 完了 |
| M2 | 夜営業の縦切り | 🟡 ほぼ完了。ループ自体は通しで動くが、会話は本番執筆前のダミー1本固定 |
| M3 | 昼パート | 🟡 着手。行き先選択→市場→仕入れ→仕込み反映、の最小縦切りが通った。金欠対応・価格調整・行き先追加はこれから |
| M4〜M6 | 未着手 | - |

### 保留になっている既知のギャップ・TODO

- **会話は `d1_roujin` ダミー1本を全客・全日で固定再生**。客ごと・日ごとのタイムライン出し分け（設計書7章 `d{日}_{客id}` 規約）と本文執筆、会話分岐（reputation/story_flags同期）は未実装。CLAUDE.mdのTODOに記録済み
- **提供後のリアクション会話も未実装**（BAD時のヒント台詞含む。設計書9章）
- **市場で何も買わずに「買い物を終える」を押すと、仕込み画面で在庫0でクラッシュする**。金欠→残り湯ロジックが未実装なため（意図的に後回し）
- **行き先は市場1つのみ**。複数行き先・キャラ会話イベントの追加は「行き先選択画面にボタンを足す」だけで拡張できる作りにしてあるが、中身は未着手
- ベース以外（トッピング・薬味）は在庫連動しておらず、無条件で選べるまま
- 価格（豚骨800／精進500）はプレースホルダ。バランス調整は未実施

### 次回の候補（次の一手）

- **金欠→残り湯の実装**（市場で何も買えない/買わない場合のフォールバック。今日の既知ギャップの解消でもある）
- **M4: 7日ループ・セーブ**（`save_manager.gd` は中身が空のまま）
- **会話の本格化**（客ごと・日ごとのタイムライン、リアクション会話、分岐）
- いずれから着手するかは次回セッション開始時に相談して決める

---

### 追記（同日・後半セッション）: 金欠→残り湯フォールバック実装、M3完了

**やったこと**

1. **残り湯フォールバックを実装し、既知クラッシュバグを解消**
   市場で何も買わず仕込みへ進むと在庫0で `game.gd` が空配列アクセス（`_bases[_base_index]`）でクラッシュしていた問題を、単なるガードではなく設計書のM3完了条件「金欠→残り湯」として実装した。
   - `data/recipes/base/base_zanryu.tres` 新規作成（残り湯ベース。rich=1/light=1/umami=1、price=0、base_volume=4、全属性最低）
   - `scenes/night/game.gd`: 在庫ベースが空のとき残り湯ベースを1件だけ入れて `_is_zanryu=true` で営業へ。`_on_setup()` では残り湯時は在庫減算をスキップ。`_zanryu_available()`（現状は常に true。M4で同日再訪判定を入れる差し替えポイント）を用意
   - `_bases` が空になる論理経路を塞いだので、既知だった `:80`（0除算）`:85` `:152`（空アクセス）が全て解消
   - `scenes/market/DestinationSelect.tscn` / `destination_select.gd`: 「残り湯: 残り湯 / 残量 4杯」ラベルを追加

2. **休業分岐は骨組みのみ（到達不能コード）**
   「在庫0＆残り湯も使えない → `end_day()` で休業」の else 節を構造として置いたが、今回「その日限り」設計のため残り湯は毎日 volume=4 で復活し、この分岐には現状フローでは到達しない。M4で「残り湯使用済みフラグ」を足せば有効化できる骨組みとして残してある。

3. **回帰テスト実施（ヘッドレスで NightScene 直接駆動、テスト後ファイル削除）**
   - ケース1（通常仕入れ→営業）: bases=[base_tonkotsu]、在庫1→0に消費、SERVING到達 PASS
   - ケース2（何も買わず→残り湯営業）: bases=[base_zanryu]、inventory={} のまま pot.volume=4 で SERVING到達 PASS
   - 両ケースともクラッシュなし。残り湯使用時に在庫が変化しないことも確認

**マイルストーン状況更新**

| # | 内容 | 状況 |
|---|---|---|
| M3 | 昼パート | ✅ 完了。市場・仕込み・金欠→残り湯が動く。設計書のM3完了条件を満たした |

（M3完了条件のうち残る「価格バランス調整」「トッピング/薬味の在庫連動」は設計書のM3スコープ外＝M5ポリッシュ相当のため、M3完了の判定には含めない）

**次の一手**
M4（セーブ・7日ループ）へ。`save_manager.gd` は中身が空のまま。後述のクズ食材脱出動線もM4スコープに含めて設計する。

---

### 将来案（M4以降で実装）: 困窮脱出動線（クズ食材ベース）

**解決したい問題**
「その日限り」残り湯だけでは、「所持金0 ＆ ベース素材0」が翌日以降も継続する状況（休業しても状態が変わらず脱出不能）を救えない。完全な詰みを回避する脱出ハッチが必要。

**案**
- 移動先選択で「クズ野菜」「くず骨」「水」を取得できる場所を追加（水は月初に一括支払い）
- 仕込み時、これらから「クズ食材ベース」（昨夜の鍋の残り ＋ 水 ＋ クズ野菜/くず骨）を作成可能にする。ステータスは最低クラスのベース
- クズ食材の取得は「所持金0 ＆ ベース素材0」のときのみ許可（平時に使える無限リソースにはしない＝ソフトフェイル救済に限定）

**なぜM4以降か（実装前提）**
1. 「昨夜の鍋の残りを持ち越す」＝ 鍋 volume/attrs の日またぎ保存が必要 → セーブ実装（M4）と密結合
2. 「所持金0＆素材0の継続」判定 ＝ 日またぎ状態管理が前提
3. 取得条件フラグも日サイクルの上に乗る

**M3の残り湯との違い（混同注意）**
- 残り湯（base_zanryu・M3実装済）: 単日の買い忘れ救済、毎日リセット
- クズ食材ベース（本案・M4以降）: 継続的困窮からの脱出動線、取得条件つき・持ち越しあり

M4のセーブ/7日ループ実装時に、この脱出動線も同時に設計・実装する。

---

### 追記（同日 or 別日）: M4ステップ4完了（Day7判定＋エンディング分岐）

**やったこと**

M4骨子の5ステップのうちステップ4（Day7判定＋エンディング専用シーン）を実装・コミット（`7b02ce6`）。

- `game_state.gd`: `enum Ending { A, B }` と受け渡し用 `last_ending`、判定純粋関数 `static func ending_for_reputation(rep)` を追加。判定は評判1軸の1段階のみ（`rep >= 5` で A、未満で B）。`save()` のホワイトリストは変更せず、`last_ending`・`phase` はともにセーブ対象外
- `scenes/home/ending.gd`（`class_name EndingScene`）＋ `Ending.tscn` を新規作成。Result と同じ流儀。A/Bで仮テキスト差し替え、ボタンはタイトルへ戻すのみ
- `result.gd`: `_handle_day7_ending()` のスタブを実装。Day7判定時に `day` がまだ7の状態で走る配線を維持（`next_day()` の前に判定）。Day7はセーブしない
- 閾値境界の使い捨てテスト（rep=5→A/4→B/0→B/100→A）で全PASSを確認後、テストファイルは削除

**設計上の判断（why）**

- 判定関数を `game_state.gd` の `static func` に置いたのは、シーンをインスタンス化せずテストできるようにするため（当初計画では `result.gd` 配置だったが、テスト容易性を優先して変更）
- エンディング結果の受け渡しは `change_scene_to_file` が引数を取れないため、`GameState.last_ending` 経由（遷移先の `_ready()` で読む）

**保留・次の一手**

- ステップ4の実機シーン遷移（Day7 Result→Ending→Title）は目視未確認。ステップ3の実機未確認分（Result配線）と合わせて、ステップ5完了後に「NEWGAME→7日→エンディング→タイトル→CONTINUE」の通しでまとめて確認する
- 残るはステップ5（タイトルの NEWGAME/CONTINUE 呼び分け）のみでM4骨子完成。`load_game()` 接続と、CONTINUE成功時に保存日の昼へ、失敗時はボタン無効化
- エンディングの本テキスト、2段目判定（謎の男の実来店＋最後の一杯GREAT）、客データ拡充・会話出し分けは引き続き骨子外

---

### 追記: M4ステップ5完了・M4骨子コンプリート

**やったこと**

ステップ5（タイトルの NEWGAME/CONTINUE 呼び分け）を実装・コミット（`da5ceff`）

- `title.gd` の `_on_load_pressed()` に `load_game()` を配線。成功時のみ DestinationSelect（昼の入口）へ。失敗（セーブ無し）時は遷移しない
- `save_manager.gd` に `has_save() -> bool`（既存 `SAVE_PATH` を再利用）を追加。`title.gd` の `_ready()` でセーブが無ければ LOAD ボタンを disabled にする（`load_game()` は `_ready()` で呼ばない＝GameState を上書きしないため `has_save()` で判定）
- `has_save()` の単体テスト（セーブ有り→true / 削除後→false）PASS、テストは後始末済み

**実機通し確認（すべてPASS）**

- エンディング分岐 A/B: debug ボタンを一時流用し（reputation=5→A / 4→B、確認後 `git checkout` で完全撤去）、Result「翌日へ」から本番配線（`_handle_day7_ending()`→`ending_for_reputation()`→Ending）を通してA/B両方の表示を目視確認
- LOAD ボタンの disabled/有効化: セーブ無し状態で disabled、セーブ作成後に有効化されることを確認
- NEWGAME→7日通し→エンディング到達、就寝時セーブでの中断再開、CONTINUE で保存日から再開、をすべて確認

これで `m4_design.md` の骨子完了条件を達成（NEWGAMEから7日・就寝時セーブ・Day7で評判に応じA/B分岐・CONTINUEで再開）。

**保留・M5以降の検討事項**

- セーブ再開位置の違和感: 「4日目の営業終了画面」から Title→LOAD すると4日目の頭に戻る。これは「就寝時（翌日へ押下時）のみセーブ、日の途中はセーブしない」方針の帰結で仕様通りだが、プレイヤーには「遊び終えた日をやり直させられる」と映りうる。M5 のセーブUX検討で、(a) Result 到達時にもセーブする（方針変更・要設計メモ）か、(b) 現状維持でセーブ日を UI 表示して期待値を揃えるか、を検討する
- Ending 画面の仮レイアウト: テキストが画面外にはみ出す・ボタンが文字に重なる。仮テキスト＋仮レイアウトのままなので M5 ポリッシュで Ending 画面を整える
- 骨子外（従来通り未着手）: 客データ拡充・来店マトリクス本実装、会話の客ごと日ごと出し分けと本文、クズ食材脱出動線、Day7エンディングの2段目判定（謎の男の実来店＋最後の一杯GREAT）

---

## 追記（2026-08-11）: セーブUX調整（Result離脱時オートセーブ）

**やったこと**

M4骨子完了時に残した「セーブ再開位置のUX調整（M5検討）」に着手し、実装（コミット `b9479db`）。

1. **`SaveManager.SAVE_PATH` → `AUTO_SAVE_PATH` にリネーム**
   将来の複数スロット拡張への布石。ロジック・保存先パスは無変更。`test_save_manager.gd` の参照も追随

2. **`result.gd` に `_finalize_day_and_save() -> bool` を新設**
   「day==7ならセーブせずfalseを返す、そうでなければ next_day()→save() してtrueを返す」処理に一本化。
   従来 `_on_next_day_pressed()` にだけあった `day==7` 判定をこの共通関数に閉じ込め、
   `_on_next_day_pressed()` と `_on_to_title_pressed()` の両方から呼ぶ形に配線し直した

3. **「タイトルへ」でもオートセーブされるように変更**
   従来「タイトルへ」はセーブを一切行っていなかった（`_on_to_title_pressed()` は無条件でTitleへ遷移するだけ）。
   これにより「N日目終了→タイトル→LOAD」で必ずN+1日目の頭に戻る一貫した挙動になった
   （従来は「翌日へ」経由のセーブしか無く、「タイトルへ」で抜けると古いセーブのまま残っていた）

4. **Day7のResultで「タイトルへ」ボタンを非表示に（案③）**
   `_ready()` で `day==7` なら `ToTitleButton.hide()`。従来は「タイトルへ」を押すとエンディング画面を経由せずタイトルに戻れてしまう抜け道があったが、これを塞いだ

5. **テスト**
   - `_finalize_day_and_save()` はノード参照・`get_tree()` に依存しないため、`result.gd` をシーンに追加せず直接インスタンス化してdayの純粋な判定部分を検証できた（使い捨てテスト、実行後削除）
   - 既存の `test_save_manager.gd`（常設）をヘッドレス実行し、リネーム後も全PASSを確認
   - 実機確認（Day3の翌日へ/タイトルへ双方からのCONTINUE、Day7のボタン非表示、Day7翌日へでの未セーブ、×終了での未セーブ）は全項目PASS

**設計変更メモ**

「就寝時セーブのみ」方針を撤回し、`docs/m4_design.md` に追記済み（詳細はそちら参照）。

**保留・次の一手**

- 困窮脱出動線（クズ食材ベース）はこの変更で土台（Result到達時点の状態保存）が整った。次に着手する場合、鍋のvolume/attrsをセーブ対象に加える形で `_finalize_day_and_save()` に乗せる
- Ending画面の仮レイアウト崩れ、客データ拡充・会話出し分け・Day7の2段目判定は引き続き未着手

---

## 追記（2026-08-11・続き）: 困窮脱出動線の土台（鍋の日またぎ持ち越し）

**やったこと**

困窮脱出動線（クズ食材ベース）の検討に着手。まず現状の棚卸しを行い、構想と現状コードの差分を把握した。

重要な発見: 現状の「残り湯」（`base_zanryu.tres`）は「その日限りの固定の薄いベース」で、構想の「前日の鍋を持ち越して薄める」とは名前が同じだけの別物だった。鍋は毎晩使い捨てで、日をまたぐと状態が消える実装になっていた。

この棚卸しを踏まえ、構想を3段階の救済ハッチとして再定義した（詳細は `docs/poverty_fallback_design.md` に記録）。そのうち全機構が共通して必要とする土台「鍋の日またぎ持ち越し」を実装・コミット（`619e93a`）。

1. **GameStateに持ち越しフィールドを追加**
   `pot_carryover_volume` / `pot_carryover_rich` / `pot_carryover_light` / `pot_carryover_umami`（すべてint）を新設。営業終了時点の鍋を加工せずそのまま保存する

2. **`night/game.gd` にスナップショット処理を追加**
   `end_day()` から `_snapshot_pot_to_gamestate()` を切り出し、`_pot` の状態をGameStateに書き込む。`_pot == null`（営業が一度も始まらなかった日）なら明示的に0を書き、前日以前の古いスナップショットが残留しないようにした

3. **save/loadに4フィールドを追加**
   `load_game()` は `.get(key, 0)` で読むため、この4フィールド追加前の旧セーブを読んでも0（持ち越しなし）で復元され、クラッシュしない

4. **今日のセーブ基盤との連携**
   `_finalize_day_and_save()` の `day==7` 判定（前回のセーブUX調整で導入済み）のおかげで、Day7の鍋はそもそも保存されない。追加のDay7ガードは不要だった

5. **テスト・実機確認**
   自動テスト（`_snapshot_pot_to_gamestate()` のシーン外テスト8項目、save/load往復＋旧形式互換13項目）全PASS。旧形式互換テストは手元に実際に残っていた旧セーブ（Day4・money3075の実プレイデータ）を使って検証した。実機でも「1杯提供ぶんvolumeが減った状態で翌日に持ち越される」辻褄が合うことを確認

**設計方針**: 鍋を素のまま持ち越すことで、「連続使用でさらに薄くなる」累積劣化を後日自然に表現できるようにする布石とした。

**保留・次の一手**

- 応用（今日のスコープ外、次回以降）: 持ち越した鍋を残り湯として薄める処理、残り湯の連続使用劣化、クズ食材ベースの入手動線、それらのUI・評価影響。詳細な仕様は `docs/poverty_fallback_design.md` に記録済み
- 棚卸しで判明した既存の負債: 現状の `base_zanryu.tres` ベースの「残り湯」は構想と別物なので、応用実装時に整理が要る
- 既存の無関係な警告（signal未使用、`ending_for_reputation` の STATIC_CALLED_ON_INSTANCE 等）がデバッガに出る。動作影響はないが、いずれ掃除する候補

---

## 追記（2026-08-12）: 会話（シナリオ）仕様の検討（棚卸し＋方針決定）

**やったこと**

会話（シナリオ）仕様の検討に着手。Dialogicの機能と現状の実装を棚卸しし、会話構想を固めた。
結論として、会話は「汎用＋特別」の2層で `.dtl` 読み込み型で持つ方針。ただし実現には現状に無い
「同席者」概念の導入が必要と判明。詳細は `docs/conversation_design.md` に記録。

**重要な発見**

1. 現状の夜営業は会話と調理が直列分離しており、コアビジョンの「ながら作業」は未実現
2. 現状は「アクティブな客1人」で複数客の同席概念がシステムに無い。会話仕様はこの同席モデルの
   導入が土台になる

**方針の転換**

「プレイヤーが調理しながら双方向会話」は煩わしいと判断し、「客同士の自律会話を横目で見る＋
メインは1人ずつ応対」に再定義した。

**保留・次の一手**

同席者モデルの導入（アクティブ1人→滞在集合1〜3人＋メイン1人）が最優先の土台工事。その後
会話タイムライン参照・選択ロジック・`story_flags`運用ルール・客データ拡充。詳細は
`docs/conversation_design.md` 参照。

---

## 追記（2026-08-12・続き）: 困窮脱出動線・残り湯（段階2）実装

**やったこと**

困窮脱出動線の応用に着手し、段階2（残り湯）を実装・コミット（`7ba66ac`）。前回の土台（鍋の日
またぎ持ち越し）の上に、実際の救済メカニズムを乗せた。

1. **`Pot.add_water()` を `add_water(times: int = 1)` に一般化**
   1回分の効果（volume+2, rich-2, umami-1、下限0ガード）を times 回重ねる形。既存呼び出し
   （`debug_pot.gd` / `test_pot.gd`）は引数省略のまま無改造で動作

2. **旧 `base_zanryu.tres`（その日限りの固定薄ベース）を廃止し、新しい残り湯に一本化**
   新しい残り湯は前日の鍋の持ち越しを希釈して使う方式。`_build_zanryu_option()` で持ち越しの
   値を持った `RecipeResource` を動的に作り、通常ベースと同じ `_bases` に混ぜる。既存の巡回選択
   UI（`%BtnSelectBase`）をそのまま流用でき、在庫があっても任意に選べる（質と引き換えの節約手段）

3. **`_on_setup()` で持ち越しから復元後、`add_water(ZANRYU_WATER_TIMES=2)` で希釈**
   希釈量は仮値で、実機調整予定

4. **評価は既存 `Evaluator`（attrs距離）でそのまま乗る**
   薄いぶん自然に評価が下がるため、残り湯用の特別な評価ロジックは不要だった

5. **テスト・実機確認**
   スモークテスト（`Pot.add_water(times)` の使い捨てテスト）全13項目PASS。実機確認も全項目PASS
   （1日目は残り湯出ない、2日目に持ち越し r=7 l=0 u=3 が表示、選ぶと `add_water(2)` で薄まる、
   在庫があっても選べる、数値の辻褄が正確に合致）

**設計時の洗練**

当初案（`Pot`に直接 `pot_carryover_*` を代入）より、持ち越しを `RecipeResource` にして既存の
`Pot.setup()` に流す方がシンプルで、`_on_setup()` の分岐が最小になり、`_refresh()` の表示も
無改造で効いた。

**保留・次の一手**

- 段階3（クズ食材）は未実装。在庫も持ち越しも残り湯も無い日は現状「暫定的に休業（`end_day()`）」。
  クズ食材実装でここを埋める
- 希釈量（`ZANRYU_WATER_TIMES=2`）と味の数値スケールは実機調整・味のバランス設計として後日
  （`docs/poverty_fallback_design.md` の派生検討課題に記録済み）
- 実機で気づいた発展: 営業中に水（薄める）とベース（濃くする）を足せれば、鍋の味を客の好みに
  寄せる調理の駆け引きになる（ベース足しは設計メモに将来仕様として記録済み）

---

## 追記（2026-08-13）: 困窮脱出動線・クズ食材（段階3）実装

**やったこと**

困窮脱出動線の最後のピース、段階3（クズ食材）を実装・実機確認まで完了。3段構造
（通常/残り湯/クズ食材）が出揃った。詳細な設計・実装の経緯は `docs/poverty_fallback_design.md`
の「段階3の実装状況」に記録。

1. **`base_kuzu.tres` を新設**
   `data/recipes/base/` にクズ食材の `RecipeResource` を追加（`Kind=BASE`, `price=0`,
   `attrs` は `rich=1/light=1/umami=0` の暫定値）。この時点ではどこからも未参照

2. **`GameState` に当日1個制限のフラグを追加**
   `junk_taken_today` / `junk_reset_day`。`junk_reset_day` と `day` の不一致で日替わり
   リセットを判定する方式。セーブ対象外（明示列挙方式の `SaveManager` のため自動混入
   しない。ロード後は day 不一致で自然にリセットされる）

3. **市場（`shop.gd`/`Shop.tscn`）に「裏手」導線を追加**
   「最安ベースが買えない（`money < _cheapest_base_price()`）＆ 通常ベース未所持
   （`_has_base_in_inventory()`）＆ 残り湯なし（`pot_carryover_volume<=0`）＆ 当日未取得」
   の4条件がすべて揃った時だけ `%BtnAlley` が表示され、押すと0円で1個 `inventory` に
   加算される。最安ベース価格は `_cheapest_base_price()` が `_bases` を走査して動的に
   求める（ハードコードしない）

4. **夜営業（`night/game.gd`）のベース選択肢に追加**
   `base_kuzu` を `_base_catalog` に追加。既存の `inventory` フィルタがそのまま効くため、
   クズ食材を持っている日だけ選択肢に出る。残り湯のような専用の仮想生成は不要だった

5. **実機確認**
   市場側（裏手ボタンの表示条件・0円入手・入手後に消える）、夜営業側（クズ食材のみが
   選択肢に出る・仕込み・提供・評価が既存 `Evaluator` にそのまま乗る）とも一時的な
   デバッグコード（`_ready()` 冒頭で所持金・在庫・持ち越しを強制する仮コード、確認後に
   撤去）を使って確認、全項目意図通り

**気づき・落とし穴**

`base_kuzu.tres` に書いていた「暫定値であることを示すコメント」が、`night/game.gd` から
`preload` されるようになった後のエディタ再スキャンでGodotがuidを自動採番するために
リソースを再シリアライズした際、カスタムコメントごと消えた。`.tres` のコメントはGodotに
一度保存し直されると保持されない前提で、設計意図はdocs側に記録する方針にした（詳細は
設計メモ参照）

**保留・次の一手**

- attrs暫定値（`rich=1/light=1/umami=0`）は「味の数値スケール設計」フェーズでの見直し対象
- 裏手ボタンのレイアウト・文言は仮
- クズ食材の会話・reputation反映は会話全面改訂フェーズの候補

残り湯の実装・テスト・実機確認まで完了。クズ食材は次回。
