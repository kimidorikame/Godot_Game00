# 夜湯（Night Broth）
## Godot 技術設計書 v0.2
対象エンジン：**Godot 4.x**（GDScript／静的型付けを推奨）
対応仕様書：MVP仕様書 v0.2

---

# 1. 設計方針

1. **データ駆動**：客・レシピ・来店スケジュールはすべて `Resource`（.tres）として定義し、インスペクタから編集する。バランス調整でコードを触らない。
2. **ロジックとUIの分離**：`Pot`・`Evaluator` は Node を継承しない純粋ロジック（`RefCounted`）にする。UI無しでテストでき、仕様変更に強い。
3. **シングルトンは最小限**：オートロードは `GameState`・`EventBus`・`SaveManager` の3つのみ。
4. **疎結合**：シーン間の通知は `EventBus` のシグナル経由。「シグナルは上へ、関数呼び出しは下へ」の原則を守る。
5. **会話は自作しない**：Dialogic 2 アドオンを採用。会話エンジンの自作はMVPのスコープ外。

---

# 2. プロジェクト構成

```
res://
├── autoload/
│   ├── game_state.gd        # 進行状態（金・評判・日数・在庫・フラグ）
│   ├── event_bus.gd         # グローバルシグナル置き場
│   └── save_manager.gd      # セーブ／ロード
├── core/                    # 純粋ロジック（Node非依存）
│   ├── pot.gd               # 鍋の状態と操作
│   ├── soup_serving.gd      # 提供する一杯（属性スナップショット）
│   └── evaluator.gd         # 評価判定
├── resources/               # Resourceクラス定義
│   ├── customer_resource.gd
│   ├── recipe_resource.gd
│   └── day_schedule_resource.gd
├── data/                    # .tres データ本体
│   ├── customers/           # 6人分
│   ├── recipes/             # ベース2・鍋食材3・トッピング3・薬味3・残り湯1
│   └── schedule/            # day1.tres 〜 day7.tres
├── dialogue/                # Dialogicタイムライン
├── scenes/
│   ├── main.tscn            # フェーズ管理・シーン切替の親
│   ├── market/              # 市場（昼）
│   ├── prep/                # 仕込み（昼）
│   ├── night/               # 夜営業
│   ├── home/                # 自宅（セーブ・翌日へ）
│   └── ui/                  # 共通UI部品
└── assets/                  # art / sfx / bgm / fonts
```

---

# 3. データ設計（Resource）

## 3.1 属性の表現

濃厚（rich）・あっさり（light）・旨味（umami）の3つの独立した整数として扱う。
Dictionaryではなく型付きの小さなクラスにして、加算・距離計算を一箇所に集約する。

```gdscript
# core/soup_attrs.gd
class_name SoupAttrs extends Resource

@export var rich: int = 0
@export var light: int = 0
@export var umami: int = 0

func add(other: SoupAttrs) -> void:
    rich += other.rich
    light += other.light
    umami += other.umami

func distance_to(other: SoupAttrs) -> int:
    return abs(rich - other.rich) + abs(light - other.light) + abs(umami - other.umami)

func duplicate_attrs() -> SoupAttrs:
    var a := SoupAttrs.new()
    a.rich = rich; a.light = light; a.umami = umami
    return a
```

## 3.2 レシピ（RecipeResource）

ベーススープ・鍋食材・トッピング・薬味を1クラスで統一し、`kind` で区別する。

```gdscript
# resources/recipe_resource.gd
class_name RecipeResource extends Resource

enum Kind { BASE, POT_INGREDIENT, TOPPING, YAKUMI }

@export var id: StringName
@export var display_name: String
@export var kind: Kind
@export var attrs: SoupAttrs          # 属性への寄与
@export var price: int                # 市場での仕入れ値
@export var base_volume: int = 8      # kind == BASE のときの仕込み杯数
@export var icon: Texture2D
```

データ例：
| id | kind | rich | light | umami | 備考 |
|---|---|---|---|---|---|
| base_tonkotsu | BASE | 5 | 0 | 3 | 白濁スープ・8杯 |
| base_shojin | BASE | 0 | 5 | 2 | 精進スープ・8杯 |
| base_leftover | BASE | 0 | 1 | 0 | 残り湯（金欠時自動選択・仕入れ不要） |
| ing_shiitake | POT_INGREDIENT | 0 | 0 | +2 | |
| top_youtiao | TOPPING | +1 | 0 | 0 | |
| yak_kurozu | YAKUMI | -1 | +1 | 0 | 黒酢：濃厚を切る |

※数値はプレースホルダ。`.tres` 上で調整する。

## 3.3 客（CustomerResource）

```gdscript
# resources/customer_resource.gd
class_name CustomerResource extends Resource

@export var id: StringName                 # "roujin", "haitatsuin" など
@export var display_name: String
@export var ideal: SoupAttrs               # 理想値
@export var great_threshold: int = 2       # 差の合計がこれ以下なら大満足
@export var ok_threshold: int = 5          # これ以下なら普通、超えたら不満
@export var base_price: int                # 定価
@export var tip: int                       # 大満足時の上乗せ
@export var portrait: Texture2D            # シルエット基本
@export var portrait_variants: Array[Texture2D]  # 差分2枚
@export var is_pro_critic: bool = false    # 元料理人フラグ（具体ヒント特権）
@export var hint_lines: Dictionary = {}    # 後述 9.2 のキー → 台詞配列
```

## 3.4 来店スケジュール（DayScheduleResource）

```gdscript
# resources/day_schedule_resource.gd
class_name DayScheduleResource extends Resource

@export var day: int
@export var visitors: Array[StringName]        # 来店順の客id（3件）
@export var previewed: Array[StringName]       # 前日に予告される2人
@export var note: String                       # 制作メモ（Day5=鉢合わせ 等）
```

Day 7 のみ特殊：3人目の「謎の男」は評判閾値チェックを通過した場合のみ出現させる（ロジック側 8.3 参照）。

---

# 4. コアロジック

## 4.1 鍋（Pot）

```gdscript
# core/pot.gd
class_name Pot extends RefCounted

const KONIZUMARI_PER_TURN := 1   # 煮詰まり：毎ターン濃厚+1
const WATER_VOLUME := 2
const WATER_RICH := -2
const WATER_UMAMI := -1

var volume: int = 0
var attrs: SoupAttrs = SoupAttrs.new()

func setup(base: RecipeResource, pot_ingredients: Array[RecipeResource]) -> void:
    attrs = base.attrs.duplicate_attrs()
    for ing in pot_ingredients:
        attrs.add(ing.attrs)
    volume = base.base_volume

func serve(topping: RecipeResource, yakumi: RecipeResource) -> SoupServing:
    assert(volume > 0)
    volume -= 1
    var cup := SoupServing.new()
    cup.attrs = attrs.duplicate_attrs()
    if topping: cup.attrs.add(topping.attrs)
    if yakumi:  cup.attrs.add(yakumi.attrs)
    cup.topping = topping
    cup.yakumi = yakumi
    return cup

func on_turn_end() -> void:
    attrs.rich += KONIZUMARI_PER_TURN

func add_water() -> void:
    volume += WATER_VOLUME
    attrs.rich = maxi(attrs.rich + WATER_RICH, 0)
    attrs.umami = maxi(attrs.umami + WATER_UMAMI, 0)

func is_empty() -> bool:
    return volume <= 0
```

```gdscript
# core/soup_serving.gd
class_name SoupServing extends RefCounted
var attrs: SoupAttrs
var topping: RecipeResource
var yakumi: RecipeResource
```

## 4.2 評価（Evaluator）

```gdscript
# core/evaluator.gd
class_name Evaluator extends RefCounted

enum Grade { GREAT, OK, BAD }

class Result extends RefCounted:
    var grade: Grade
    var worst_axis: StringName    # "rich" / "light" / "umami"
    var over: bool                # true=過剰, false=不足
    var payment: int
    var rep_delta: int

static func evaluate(cup: SoupServing, customer: CustomerResource) -> Result:
    var r := Result.new()
    var d := cup.attrs.distance_to(customer.ideal)
    if d <= customer.great_threshold:
        r.grade = Grade.GREAT
        r.payment = customer.base_price + customer.tip
        r.rep_delta = 1
    elif d <= customer.ok_threshold:
        r.grade = Grade.OK
        r.payment = customer.base_price
        r.rep_delta = 0
    else:
        r.grade = Grade.BAD
        r.payment = int(customer.base_price * 0.5)
        r.rep_delta = -1
    _fill_worst_axis(r, cup, customer)
    return r

static func _fill_worst_axis(r: Result, cup: SoupServing, c: CustomerResource) -> void:
    var diffs := {
        &"rich":  cup.attrs.rich  - c.ideal.rich,
        &"light": cup.attrs.light - c.ideal.light,
        &"umami": cup.attrs.umami - c.ideal.umami,
    }
    var worst: StringName = &"rich"
    for k in diffs:
        if abs(diffs[k]) > abs(diffs[worst]):
            worst = k
    r.worst_axis = worst
    r.over = diffs[worst] > 0
```

不満時のヒント台詞は `worst_axis + (over ? "_over" : "_under")` をキーに `CustomerResource.hint_lines` から引く（例：`rich_over` →「胃にもたれるな…」）。キーが無ければ共通テンプレにフォールバックする。

---

# 5. オートロード

## 5.1 GameState

```gdscript
# autoload/game_state.gd
extends Node

enum Phase { MORNING, MARKET, PREP, NIGHT, RESULT, HOME }

const REP_ENDING_THRESHOLD := 5      # 謎の男が来る評判閾値（要調整）

var day: int = 1
var money: int = 100
var reputation: int = 0
var inventory: Dictionary = {}       # RecipeResource.id -> 個数
var story_flags: Dictionary = {}     # StringName -> bool（会話分岐用）
var phase: Phase = Phase.MORNING
var pot: Pot = null                  # 当日の鍋（夜のみ有効）

func can_afford_cheapest_base() -> bool:
    # 金欠判定：どのベースも買えない → 残り湯営業
    ...

func apply_result(result: Evaluator.Result) -> void:
    money += result.payment
    reputation += result.rep_delta
    EventBus.reputation_changed.emit(reputation)
```

## 5.2 EventBus

```gdscript
# autoload/event_bus.gd
extends Node

signal phase_changed(phase: int)
signal soup_served(cup: SoupServing, result: RefCounted)
signal water_added
signal pot_emptied
signal customer_arrived(customer: CustomerResource)
signal customer_left(customer: CustomerResource)
signal reputation_changed(value: int)
signal day_ended(day: int)
```

## 5.3 SaveManager

- 保存先：`user://save.json`（JSON・`version` フィールド付き）
- セーブは自宅でのみ実行。保存内容は `day / money / reputation / inventory / story_flags` のみ（鍋の状態は日をまたがないため保存不要）
- ロード時は `version` を見てマイグレーション（MVPでは version 1 固定でよいが、フィールドだけ用意しておく）

---

# 6. シーン構成とゲームフロー

## 6.1 フェーズ遷移

`main.tscn` の `Main.gd` がステートマシンとして機能し、フェーズに応じて子シーンを差し替える。

```
MORNING（日付表示・予告の回想）
   ↓
MARKET（仕入れ）──金欠なら自動スキップ→ 残り湯フラグON
   ↓
PREP（ベース選択・鍋食材投入 → Pot.setup）
   ↓
NIGHT（夜営業：ターンループ）
   ↓
RESULT（売上・評判の精算表示）
   ↓
HOME（セーブ → day += 1 → MORNINGへ）
   ※ day > 7 ならエンディング判定へ
```

シーン切替は `Main.gd` の `change_phase(phase)` に集約。各フェーズシーンは終了時に `phase_finished` シグナルを発火するだけで、次にどこへ行くかを知らない（遷移の知識をMainに一元化）。

## 6.2 夜営業のターンループ（NightService.gd）

```
1. DayScheduleResource(day N) を読み込む
2. for visitor_id in visitors:
     a. customer_arrived 発火 → 入店演出
     b. Dialogic タイムライン "d{N}_{visitor_id}" 再生（注文前会話）
     c. 提供UI表示：
        - トッピング選択（在庫にあるもののみ）
        - 薬味選択（同上）
        - [水を差す] ボタン（押すと Pot.add_water() → 鍋演出更新 → UIに戻る）
        - [提供] ボタン
     d. Pot.serve() → Evaluator.evaluate()
     e. リアクション再生：
        - grade に応じた共通演出（湯気・SE・アイコン）
        - grade == BAD なら hint_lines から台詞表示
        - 節目の日なら追加の物語断片タイムライン再生
     f. GameState.apply_result() → Pot.on_turn_end()
     g. Pot.is_empty() なら残りの客をスキップ（「売り切れ」演出）→ ループ終了
3. day_ended 発火 → RESULT へ
```

## 6.3 Day 7 とエンディング判定

```gdscript
# NightService 内・Day7のvisitor展開時
var visitors := schedule.visitors.duplicate()
if GameState.reputation < GameState.REP_ENDING_THRESHOLD:
    visitors.erase(&"nazo_no_otoko")   # 謎の男は来ない

# 営業終了後（day == 7）
func decide_ending() -> StringName:
    if not _nazo_visited:
        return &"ending_b"
    return &"ending_a" if _nazo_result.grade == Evaluator.Grade.GREAT else &"ending_b"
```

---

# 7. Dialogic 2 との連携

- **命名規約**：タイムラインは `d{日}_{客id}`（例：`d3_keiji`）。エンディングは `ending_a` / `ending_b`。
- **変数同期**：`reputation` と `story_flags` はタイムライン開始前に Dialogic 変数へ書き込み、終了時に読み戻す（会話内分岐に使用）。
- **ゲーム側への通知**：Dialogic のシグナルイベントを使い、`Dialogic.signal_event` を NightService が受けて演出（SE・エフェクト）を発火する。
- **リアクション台詞**：3段階の共通リアクションは Dialogic に入れず、ゲーム側のテンプレ（Label＋タイプライタ表示）で処理する。Dialogic は「物語」、テンプレは「システム反応」と役割を分ける。ヒント台詞もテンプレ側。

---

# 8. UI設計（夜画面）

```
┌──────────────────────────────┐
│  背景：屋台（夜）                          │
│            ［客のシルエット立ち絵］          │
│  ┌─────┐                              │
│  │ 鍋    │   ← 色・とろみ・湯気で濃度を表現   │
│  │ ▓▓▓░░ │   ← 残量ゲージ（数値明示OK）      │
│  └─────┘                              │
│ ┌──────────────────────────┐ │
│ │ 会話ウィンドウ／リアクション表示            │ │
│ └──────────────────────────┘ │
│ [トッピング▼] [薬味▼] [水を差す] [提供する]      │
└──────────────────────────────┘
```

- 鍋の見た目：`rich` 値を3〜4段階に量子化し、スープのシェーダ／スプライト差分と湯気パーティクル量を切り替える（数値は出さない）
- UIノードは入力を受けたらシグナルを上げるだけで、Pot を直接触らない（NightService が仲介）

---

# 9. テストとバランス調整

- `Pot` と `Evaluator` は Node 非依存なので、GUT（Godot Unit Test）または起動時 assert スクリプトで検証できる。最低限のテスト項目：
  - 煮詰まりがターンごとに +1 されること
  - add_water の3効果と下限0クランプ
  - 閾値境界（great/ok/bad の境目）の判定
  - Day7 で評判不足時に謎の男が除外されること
- バランス調整は `.tres` の数値のみで完結させる。スプレッドシートで「7日×3人の理想値と、その日の鍋の想定属性推移」を一覧化してから `.tres` に転記すると破綻に気付きやすい。

---

# 10. 実装マイルストーン

| # | 内容 | 完了条件 |
|---|---|---|
| M1 | コアロジック | Pot / Evaluator / SoupAttrs が完成し、UI無しのテストスクリプトで仕様通り動く |
| M2 | 夜営業の縦切り | プレースホルダ絵＋Dialogic導入で「1晩・客3人」が通しでプレイできる |
| M3 | 昼パート | 市場・仕込み・在庫・金欠→残り湯が動き、昼→夜が繋がる |
| M4 | 7日ループ | スケジュール7日分・セーブ・エンディング分岐まで通しプレイ可能 |
| M5 | ポリッシュ | 本番素材差し替え・SE/BGM・演出・タイトル/リザルト画面 |
| M6 | 体験版切り出し | Day1〜3＋客4人に制限したビルドを書き出し（Steam Next Fest用） |

M1→M2 の順番が重要。**絵もUIも無い段階で鍋と評価の手触りを数値で確認**してから画面を作ると、手戻りが最小になります。

---

# 11. 規約メモ

- GDScript は静的型付け（`var x: int` / 戻り値型）を徹底。`class_name` を全クラスに付与
- ファイル名 snake_case、クラス名 PascalCase、シグナル名は過去形（`soup_served`）
- マジックナンバー禁止：閾値・係数はすべて `const` か `.tres` に置く
- Git 管理。`.godot/` は .gitignore へ
